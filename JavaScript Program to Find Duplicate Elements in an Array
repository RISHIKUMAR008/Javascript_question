const a  = [1, 2, 3, 4, 5, 2, 3, 6];
let dupli = [];

for (let i in a) {
    for (let j in a) {
        if (i !== j && a[i] === a[j] && !dupli.includes(a[i])) {
            dupli.push(a[i]);
        }
    }
}

console.log(dupli);
