function findUnique(arr) {
    let res = 0;
    
    // Find XOR of all elements
    for (let i = 0; i < arr.length; i++) {
        res ^= arr[i];
    }
    
    return res;
}

const arr = [ 2, 2, 5, 5, 20, 30, 30 ];
console.log(findUnique(arr));
