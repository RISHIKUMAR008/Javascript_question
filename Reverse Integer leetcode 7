var reverse = function(x) {
     let str=String(x).split("").reverse().join("")
    if(Number.parseInt(str)>=(Math.pow(2,31)-1)||Number.parseInt(str)<=Math.pow(-2,31)){
        return 0
    }
  
    else{
        
    return x < 0 ? Number.parseInt(`-${str}`) : Number.parseInt(str)
    }
}
